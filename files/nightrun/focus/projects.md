# Project focus

Add repositories that unattended runs may inspect. Use paths below
`~/workspace/playground` and choose the minimum autonomy needed.

## Example project [autonomy: pull-request]

- **Repo:** `~/workspace/playground/example-project`
- **Goal:** replace with a concrete, measurable outcome.
- **Verification:** replace with the required test and build commands.
- **Constraints:** replace with project-specific boundaries.
- **State file:** `~/.nightshift/state/example-project.md`

Valid autonomy values are `review-only`, `pull-request`, `merge`, and `deploy`.
Remove this example if the repository does not exist.
