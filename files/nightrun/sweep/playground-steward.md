# Playground steward

Read `~/.nightshift/focus/projects.md`, current tickets, recent reports, and
project state files. Produce `~/.nightshift/briefs/playground.md` with a ranked
list of concrete work that fits the configured autonomy and can be verified in
one run.

Do not invent repository access, deployment authority, or project goals that
are not written in the focus file.
